# Production Environment (Placeholder)

Copy `../dev` composition and adjust:

- `environment = "prod"`
- stricter IAM and network policies
- high-availability sizing
- mandatory remote state backend
- change controls through CI with manual approval
