apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: booking-app
spec:
  selector:
    app.kubernetes.io/name: agent
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP
