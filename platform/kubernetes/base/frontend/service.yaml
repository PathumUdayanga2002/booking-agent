apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: booking-app
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
  type: ClusterIP
